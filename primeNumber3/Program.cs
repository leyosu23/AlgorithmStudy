using System;
//https://www.acmicpc.net/problem/1929
/*
 * Natural numbers M,N are given to the first line separated by a single space.
 * Print prime numbers line by line between them in ascending order.
 * ( m always be the small number than n or equal to n)
 */
namespace primeNumber3
{
    class Program
    {
        static void Main(string[] args)
        {
            #region loop
            //string nums = Console.ReadLine();
            //string[] str = nums.Split();
            //int m = int.Parse(str[0]);
            //int n = int.Parse(str[1]);
            //bool isPrime = false;

            //// sum of prime numbers
            //while (m <= n)
            //{
            //    // run for loop n times
            //    int num = m;
            //    int curr = num - 1;
            //    if (num > 2)
            //    {
            //        while (curr > 1)
            //        {
            //            isPrime = true;
            //            // isPrime gets false if number has common factor
            //            if (num % curr == 0)
            //            {
            //                isPrime = false;
            //                break;
            //            }
            //            curr--;
            //        }

            //        //check this number is prime number
            //        if (isPrime)
            //        {
            //            Console.WriteLine(m);
            //        }
            //    }
            //    else if (num == 2)
            //    {
            //        Console.WriteLine(m);
            //    }
            //    m++;
            //}
            #endregion





        }
}
}
